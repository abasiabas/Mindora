// Placeholder hand-written types. Once the Supabase project exists, replace
// this file by running:
//   npx supabase gen types typescript --project-id <your-project-id> > src/types/database.ts
// Keeping `Database = any` here is intentional and temporary — it unblocks
// development without faking real generated types (bind 76: no fake completion).
export type Database = any;
